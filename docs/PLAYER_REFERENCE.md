# ONI PIN — プレイヤー向けリファレンス（現行仕様）

> **一次ソース（コード）:** `lib/game/skill_reference.dart`（装備スキル）, `lib/game/game_config.dart`（数値）, `lib/game/werewolf_forced_schedule.dart`（人狼タイマー）  
> **アプリ内:** 遊び方シート「スキル」「役職」「詳細ルール」章  
> **リリースノート用:** 本書の各節をそのまま抜粋可能

---

## 装備スキル（5種）

| スキル | 誰が | 一言 |
|--------|------|------|
| 偽位置 | 逃走者 | 約20秒、すべての暴露がおとり近くに出る |
| 偽情報暴露 | 鬼 | 本物そっくりの名前付き暴露を地図に置く |
| 体投げ | 鬼 | 人形の位置を基準に捕獲・暴露が動く |
| 捕獲結界 | 逃走者・鬼 | 範囲内の全員を拘束。効果中のみ離脱リスク |
| 鬼化・人化 | 人狼 | 姿を切り替えて陣営に応じた追跡・拘束 |

### 偽位置（逃走者）

- 進行方向の先（約38m）におとり。約20秒持続、再使用まで約72秒。
- **名前付き・匿名・定期暴露はすべておとり近傍**に出る。
- 鬼の体投げ稼働中は人形側が優先。

### 偽情報暴露（鬼）

- 自分 or ランダム逃走者を選び、プレイエリア内に名前付き暴露を設置。
- 相手からは偽とは分からない（理由タグなし）。匿名痕跡ではない。
- 設置成功後、再使用まで約75秒。

### 体投げ（鬼）

- 約90m以内に人形を設置。回収まで捕獲・暴露の基準は人形。
- 約12秒以内に約15m以内で回収しないと人形位置が名前付き暴露。
- 人形稼働中は**他スキル不可**。回収後、再使用まで約75秒。

### 捕獲結界

- 半径約55m・持続約24秒・再使用まで約80秒。設置射程約90m。
- 発動時に範囲内にいた**全員**（自分・味方・敵）を拘束。
- **効果中**に円外へ出ると名前付き暴露（猶予約10秒後、捕獲可能なら脱落）。
- **終了後**は結界消滅・拘束解除。旧位置から離れても暴露なし。
- 鬼陣営＋鬼化中の人狼の結界は捕獲不可（パニック・拘束のみ）。

### 鬼化・人化（人狼）

- **自動切替**（旧:強制まで）と**切替可能まで**（旧:切替CD）は別タイマー（HUDに別表示）。
- **自動切替:** 前回の切替（自動・任意どちらでも）から `min(15分, 試合時間÷2)` で自動トグル（通知なし）。
- **切替可能まで:** 任意にボタンで切り替えられるまでの待ち＝自動切替間隔÷3。
- **自動切替の直後**は次に切り替えられるまでがやや長い（任意待ち + 任意待ち÷2）。
- 人陣営＋鬼化＝捕獲可。鬼陣営＋鬼化＝パニック・拘束のみ。告発不可。

---

## パニック（感染）

- 接触圏より外側の環に約6秒 → 約22秒のパニック状態。
- 約7秒ごとに**匿名痕跡**（名前付き暴露ではない）。脱落なし。
- 偽位置中は痕跡もおとり近傍。

---

## 接触拘束・捕獲（鬼の基本）

- 接触圏（約35〜95m）に約4秒 → 接触拘束（円約45〜110m）。
- GPS約12m or BLE接触で即捕獲。拘束円外約10秒で暴露→捕獲。

**スキル捕獲結界とは別物。**

---

## 情報の読み方

| 種類 | 意味 |
|------|------|
| 名前付き暴露 | 「○○がここにいた」。信頼度高いが今もそことは限らない |
| 匿名痕跡（?） | 「誰かがいた」。量で補う |
| 定期匿名 | 試合時間に連動した間隔で1人が匿名暴露 |

---

## ドキュメントの整理（2026-06）

| 読むもの | 用途 |
|----------|------|
| **本書** | プレイヤー・リリースノート・QA |
| `lib/game/skill_reference.dart` | 装備スキルの文案・数値表（コードと同期） |
| `docs/GAME_DESIGN_SPEC.md` | 設計者向けの接近3系統・告発・第二ゲーム |
| `docs/HOWTO_GENERATION_SPEC.md` | GPT向けの実装準拠仕様（本書と矛盾時はコード優先） |
| `docs/how_to_play_v2/` | UI設計メモ（**仕様の一次ソースではない**） |

`docs/how_to_play_v2/04_GuideBook.md` 等の長文はアプリ内ガイドに統合済み。更新は `skill_reference.dart` と `guide_sections.dart` を優先してください。
